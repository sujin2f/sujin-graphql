query focusDevices($page: Int!) {
    focusDevices(page: $page) {
        _id
        device
        machineId
    }
}

query {
    page(slug: "about") {
        title
    }

    postsAllAdmin(page: 1) {
        title
    }
}
