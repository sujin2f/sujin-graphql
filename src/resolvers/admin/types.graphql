type Query {
    focusDevices(page: Int!): [FocusDevice]
    pages(page: Int!): [Page]
    postsAllAdmin(page: Int!): [Post]
}

type Mutation {
    focusRemoveDevices: Boolean
}

type FocusDevice {
    _id: String
    device: String
    machineId: String
}
